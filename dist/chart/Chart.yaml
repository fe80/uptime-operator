apiVersion: v2
name: uptime-operator
description: A Helm chart to distribute uptime-operator
type: application

# Placeholders; release.yml stamps real values via `helm package --version $TAG --app-version $TAG`.
# `kubebuilder edit --plugins=helm/v2-alpha --force` regenerates this file and will overwrite these
# back to the plugin defaults - restore the `0.0.0-dev` placeholders after running it.
version: 0.0.0-dev
appVersion: "0.0.0-dev"

keywords:
  - kubernetes
  - operator

annotations:
  kubebuilder.io/generated-by: kubebuilder
