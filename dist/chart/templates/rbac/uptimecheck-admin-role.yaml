{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "uptime-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "uptime-operator.resourceName" (dict "suffix" "uptimecheck-admin-role" "context" $) }}
rules:
- apiGroups:
  - monitoring.uptime.com
  resources:
  - uptimechecks
  verbs:
  - '*'
- apiGroups:
  - monitoring.uptime.com
  resources:
  - uptimechecks/status
  verbs:
  - get
{{- end }}
